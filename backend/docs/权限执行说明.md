# Camera 项目权限机制执行说明

## 一、快速执行（推荐）

### 1. 执行 SQL 脚本

```bash
# 方式 1：使用执行脚本（推荐）
cd /root/.openclaw/workspace/OpenClaw_Camera_Construction_Mgmt/backend/docs
./execute-permissions.sh

# 方式 2：直接执行
PGPASSWORD='uBUu8wZ50COo/1bhJwBpAIP/PKOFKutUnqdGH1dp3yw=' \
  psql -h localhost -U postgres -d camera_construction_db \
  -f /root/.openclaw/workspace/OpenClaw_Camera_Construction_Mgmt/backend/docs/permissions.sql
```

### 2. 添加角色权限配置菜单

```bash
PGPASSWORD='uBUu8wZ50COo/1bhJwBpAIP/PKOFKutUnqdGH1dp3yw=' \
  psql -h localhost -U postgres -d camera_construction_db <<EOF

-- 添加角色权限配置菜单
INSERT INTO menus (menu_code, menu_name, menu_path, parent_id, sort_order, icon, is_visible, required_permission)
VALUES ('role_permission', '角色权限配置', '/system/role-permission', NULL, 7, 'Setting', true, 'menu:user:edit')
ON CONFLICT (menu_code) DO NOTHING;

-- 为系统管理员添加菜单权限
INSERT INTO role_menu_permissions (role_id, menu_id, can_view, can_operate)
SELECT 1, (SELECT id FROM menus WHERE menu_code = 'role_permission'), true, true
ON CONFLICT (role_id, menu_id) DO NOTHING;

-- 验证菜单已添加
SELECT id, menu_code, menu_name, menu_path FROM menus WHERE menu_code = 'role_permission';
EOF
```

### 3. 重启后端服务

```bash
# 停止服务（根据实际部署方式）
# 方式 1：如果是 systemd 服务
sudo systemctl stop camera-system

# 方式 2：如果是 docker
docker-compose -f /root/.openclaw/workspace/OpenClaw_Camera_Construction_Mgmt/docker-compose.yml stop backend

# 方式 3：如果是直接运行，Ctrl+C 停止

# 重新编译
cd /root/.openclaw/workspace/OpenClaw_Camera_Construction_Mgmt/backend
mvn clean package -DskipTests

# 启动服务
# 方式 1：systemd
sudo systemctl start camera-system

# 方式 2：docker
docker-compose -f /root/.openclaw/workspace/OpenClaw_Camera_Construction_Mgmt/docker-compose.yml up -d backend

# 方式 3：直接运行
java -jar target/camera-system-1.0.0.jar
```

### 4. 验证

1. 使用系统管理员账号登录
2. 访问 `http://your-domain/system/role-permission`
3. 选择角色并配置权限
4. 保存后使用其他角色登录验证

---

## 二、分步详细说明

### 步骤 1：数据库准备

#### 1.1 检查数据库连接

```bash
PGPASSWORD='uBUu8wZ50COo/1bhJwBpAIP/PKOFKutUnqdGH1dp3yw=' \
  psql -h localhost -U postgres -d camera_construction_db -c "SELECT version();"
```

#### 1.2 备份现有数据（可选但推荐）

```bash
PGPASSWORD='uBUu8wZ50COo/1bhJwBpAIP/PKOFKutUnqdGH1dp3yw=' \
  pg_dump -h localhost -U postgres -d camera_construction_db \
  --table=permissions --table=role_permissions --table=role_menu_permissions \
  -f /tmp/permissions_backup_$(date +%Y%m%d_%H%M%S).sql
```

#### 1.3 执行权限 SQL 脚本

```bash
# 执行脚本
./execute-permissions.sh

# 或手动执行
PGPASSWORD='uBUu8wZ50COo/1bhJwBpAIP/PKOFKutUnqdGH1dp3yw=' \
  psql -h localhost -U postgres -d camera_construction_db \
  -f /root/.openclaw/workspace/OpenClaw_Camera_Construction_Mgmt/backend/docs/permissions.sql
```

#### 1.4 验证执行结果

```bash
PGPASSWORD='uBUu8wZ50COo/1bhJwBpAIP/PKOFKutUnqdGH1dp3yw=' \
  psql -h localhost -U postgres -d camera_construction_db <<EOF
-- 查看各表数据量
SELECT 'permissions' AS table_name, COUNT(*) AS count FROM permissions
UNION ALL
SELECT 'api_permissions', COUNT(*) FROM api_permissions
UNION ALL
SELECT 'ui_element_permissions', COUNT(*) FROM ui_element_permissions
UNION ALL
SELECT 'role_permissions', COUNT(*) FROM role_permissions
UNION ALL
SELECT 'role_menu_permissions', COUNT(*) FROM role_menu_permissions;

-- 查看各角色的权限数量
SELECT r.id, r.role_name, COUNT(rp.permission_id) AS permission_count
FROM roles r
LEFT JOIN role_permissions rp ON r.id = rp.role_id
GROUP BY r.id, r.role_name
ORDER BY r.id;
EOF
```

**预期输出**：
- permissions: 47
- api_permissions: 47
- ui_element_permissions: 50+
- role_permissions:  varies by role
- role_menu_permissions: varies by role

---

### 步骤 2：添加菜单

```bash
PGPASSWORD='uBUu8wZ50COo/1bhJwBpAIP/PKOFKutUnqdGH1dp3yw=' \
  psql -h localhost -U postgres -d camera_construction_db <<EOF
-- 添加角色权限配置菜单
INSERT INTO menus (menu_code, menu_name, menu_path, parent_id, sort_order, icon, is_visible, required_permission)
VALUES ('role_permission', '角色权限配置', '/system/role-permission', NULL, 7, 'Setting', true, 'menu:user:edit')
ON CONFLICT (menu_code) DO NOTHING;

-- 为系统管理员添加菜单权限
INSERT INTO role_menu_permissions (role_id, menu_id, can_view, can_operate)
SELECT 1, (SELECT id FROM menus WHERE menu_code = 'role_permission'), true, true
ON CONFLICT (role_id, menu_id) DO NOTHING;

-- 验证
SELECT id, menu_code, menu_name, menu_path FROM menus WHERE menu_code = 'role_permission';
EOF
```

---

### 步骤 3：编译后端

```bash
cd /root/.openclaw/workspace/OpenClaw_Camera_Construction_Mgmt/backend

# 清理并编译
mvn clean package -DskipTests

# 验证编译产物
ls -lh target/*.jar
```

---

### 步骤 4：部署并重启

根据你的部署方式选择：

#### 方式 1：Docker Compose

```bash
cd /root/.openclaw/workspace/OpenClaw_Camera_Construction_Mgmt

# 重新构建并重启
docker-compose build backend
docker-compose up -d backend

# 查看日志
docker-compose logs -f backend
```

#### 方式 2：Systemd 服务

```bash
# 停止服务
sudo systemctl stop camera-system

# 替换 jar 文件
cp /root/.openclaw/workspace/OpenClaw_Camera_Construction_Mgmt/backend/target/camera-system-1.0.0.jar \
   /opt/camera-system/

# 启动服务
sudo systemctl start camera-system

# 查看状态
sudo systemctl status camera-system
```

#### 方式 3：直接运行

```bash
# 停止旧进程
pkill -f camera-system

# 启动新进程
cd /root/.openclaw/workspace/OpenClaw_Camera_Construction_Mgmt/backend
nohup java -jar target/camera-system-1.0.0.jar > /var/log/camera-system.log 2>&1 &

# 查看日志
tail -f /var/log/camera-system.log
```

---

### 步骤 5：前端部署（可选）

如果前端需要立即使用新的权限管理页面：

```bash
cd /root/.openclaw/workspace/OpenClaw_Camera_Construction_Mgmt/frontend

# 安装依赖（如果需要）
npm install

# 构建
npm run build

# 部署到 nginx 目录
# 根据实际部署路径调整
cp -r dist/* /var/www/html/
```

---

## 三、验证测试

### 3.1 系统管理员测试

1. 使用系统管理员账号登录
2. 访问 `/system/role-permission` 页面
3. 选择一个角色（如"甲方管理员"）
4. 勾选权限并保存
5. 验证保存成功

### 3.2 角色权限测试

1. 使用刚配置的角色登录
2. 尝试访问该角色有权限的页面
3. 尝试访问该角色无权限的页面（应被拒绝）
4. 验证按钮是否正确显示/隐藏

### 3.3 API 权限测试

```bash
# 使用无权限角色登录获取 token
TOKEN="your-token"

# 尝试访问无权限的 API（应返回 403）
curl -X GET "http://localhost:8080/role" \
  -H "Authorization: Bearer $TOKEN"

# 预期响应：{"code":403,"message":"无权限访问","data":null}
```

---

## 四、常见问题

### Q1: SQL 执行失败

**错误**：表已存在或权限不足

**解决**：
```bash
# 检查表是否存在
PGPASSWORD='...' psql -h localhost -U postgres -d camera_construction_db \
  -c "\dt permissions"

# 检查用户权限
PGPASSWORD='...' psql -h localhost -U postgres -d camera_construction_db \
  -c "SELECT current_user, current_database();"
```

### Q2: 编译失败

**错误**：找不到符号或依赖

**解决**：
```bash
# 清理 Maven 缓存
mvn clean

# 重新下载依赖
mvn dependency:purge-local-repository

# 重新编译
mvn clean package -DskipTests
```

### Q3: 权限不生效

**检查**：
1. 拦截器是否正确注册
2. Controller 是否添加了 `@ApiPermission` 注解
3. 用户是否重新登录（权限缓存）

**日志**：
```bash
# 查看应用日志
tail -f /var/log/camera-system.log | grep -i permission
```

---

## 五、回滚方案

如果需要回滚：

```bash
# 恢复数据库备份
PGPASSWORD='uBUu8wZ50COo/1bhJwBpAIP/PKOFKutUnqdGH1dp3yw=' \
  psql -h localhost -U postgres -d camera_construction_db \
  -f /tmp/permissions_backup_YYYYMMDD_HHMMSS.sql

# 恢复旧版本 jar
sudo systemctl stop camera-system
cp /opt/camera-system/camera-system-1.0.0.jar.backup \
   /opt/camera-system/camera-system-1.0.0.jar
sudo systemctl start camera-system
```

---

## 六、文档索引

| 文档 | 路径 |
|------|------|
| 权限控制方案 | `backend/docs/权限控制方案.html` |
| SQL 脚本 | `backend/docs/permissions.sql` |
| 执行脚本 | `backend/docs/execute-permissions.sh` |
| 实现总结 | `backend/docs/权限实现总结.md` |
| 执行说明 | `backend/docs/权限执行说明.md` (本文档) |

---

**更新日期**：2026-03-27  
**版本**：v1.0
