# 测试指南更新说明

**更新日期**: 2026-03-19 22:45  
**更新人员**: AI Assistant  
**更新原因**: 根据 API 自动化测试结果修正文档错误

---

## 📝 更新内容

### 1. 认证接口 - AUTH-002

**修正前**:
```markdown
| POST | `/api/auth/captcha` | 获取验证码 | 否 |
```

**修正后**:
```markdown
| **GET** | `/api/auth/captcha` | 获取图形验证码 | 否 |
| POST | `/api/auth/captcha/sms` | 发送短信验证码 | 否 |
| GET | `/api/auth/captcha/config` | 获取验证码配置 | 否 |
```

**原因**: 实际接口支持的方法是 GET，不是 POST

---

### 2. 用户接口 - 路径修正

**修正前**:
```markdown
| GET | `/api/users` | 用户列表 | 是 |
```

**修正后**:
```markdown
| GET | `/api/user/list` | 用户列表（分页） | 是 |
```

**完整用户接口列表**:
```markdown
| 方法 | 路径 | 描述 | 认证 |
|------|------|------|------|
| GET | `/api/user/list` | 用户列表（分页） | 是 |
| GET | `/api/user/{id}` | 用户详情 | 是 |
| POST | `/api/user` | 创建用户 | 是 |
| PUT | `/api/user/{id}` | 编辑用户 | 是 |
| DELETE | `/api/user/{id}` | 删除用户 | 是 |
| POST | `/api/user/{id}/reset-password` | 重置密码 | 是 |
| POST | `/api/user/batch-import` | 批量导入 | 是 |
| POST | `/api/user/import-template` | 下载导入模板 | 是 |
| GET | `/api/user/pending-approvals` | 待审批用户列表 | 是 |
| POST | `/api/user/approve` | 用户审批 | 是 |
| GET | `/api/user/profile` | 获取个人信息 | 是 |
| PUT | `/api/user/profile` | 修改个人信息 | 是 |
| PUT | `/api/user/profile/password` | 修改密码 | 是 |
```

**原因**: 实际 API 路径是 `/api/user/xxx`，不是 `/api/users/xxx`

---

### 3. 系统配置接口 - 路径修正

**修正前**:
```markdown
| GET | `/api/system/configs` | 获取系统配置 | 是 |
| PUT | `/api/system/configs` | 修改系统配置 | 是 |
```

**修正后**:
```markdown
| GET | `/api/system/config` | 获取系统配置 | 是 |
| PUT | `/api/system/config` | 修改系统配置 | 是 |
| GET | `/api/system/config/captcha/all` | 获取验证码相关配置 | 是 |
```

**原因**: 实际 API 路径是 `/api/system/config`（单数），不是 `/api/system/configs`（复数）

---

### 4. 个人中心接口 - 路径修正

**修正前**:
```markdown
| PUT | `/api/users/profile/password` | 修改密码 | 是 |
```

**修正后**:
```markdown
| PUT | `/api/user/password` | 修改密码 | 是 |
```

**原因**: 实际 API 路径是 `/api/user/password`，不是 `/api/users/profile/password`

**验证结果**:
- PROF-002: ✅ 通过 (PUT /api/user/password)

---

## 📊 影响范围

### 受影响的测试用例
1. **AUTH-002** - 验证码接口测试
2. **USER-001** - 用户列表分页测试
3. **SYS-001** - 系统配置查看测试

### 受影响的文档
1. ✅ 测试指南_v1.1.0_2026-03-19.md - 已更新
2. ✅ 测试执行报告 - 已记录修正

---

## ✅ 验证结果

修正后的 API 路径和方法已通过测试验证：
- AUTH-002: ✅ 通过 (GET /api/auth/captcha)
- USER-001: ✅ 通过 (GET /api/user/list)
- SYS-001: ✅ 通过 (GET /api/system/config)

---

## 📋 建议

1. **更新 Swagger 文档**: 确保在线文档与实际情况一致
2. **通知开发团队**: 确保前后端开发人员了解正确的 API 定义
3. **更新测试脚本**: 所有自动化测试脚本应使用正确的路径和方法

---

*更新说明生成时间：2026-03-19 22:45*  
*更新人员：AI Assistant*
